apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: uber
spec:
  type: NodePort
  ports:
    - port: 4000
      targetPort: 4000
      nodePort: 30090   # Expose backend on port 30090
  selector:
    app: backend
