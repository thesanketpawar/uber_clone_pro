apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: mongodb-pvc
  namespace: uber

spec:
  storageClassName: ebs-sc

  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 5Gi
